import type { FC } from "react";
import { AdminDashboardHome } from "../dashboard/AdminDashboardHome";

/** Platform analytics — real AnalyticsService overview + charts. */
export const AnalyticsPage: FC = () => <AdminDashboardHome />;
